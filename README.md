# Система автоматизации лабораторной сортировки пробирок

Система управления роботом-манипулятором Agilebot для автоматической сортировки медицинских пробирок по типам анализов с использованием QR-сканирования и интеграции с ЛИС (Лабораторной Информационной Системой).

## 📋 Содержание

- [Описание](#описание)
- [Возможности](#возможности)
- [Архитектура](#архитектура)
- [Требования](#требования)
- [Установка](#установка)
- [Быстрый старт](#быстрый-старт)
- [Конфигурация](#конфигурация)
- [Использование](#использование)
- [API](#api)
- [Структура проекта](#структура-проекта)
- [Устранение неполадок](#устранение-неполадок)

## 🎯 Описание

Система автоматизирует процесс сортировки лабораторных пробирок:
1. **Сканирование** - робот сканирует QR-коды пробирок на исходных паллетах
2. **Классификация** - система запрашивает информацию о типе анализа из ЛИС
3. **Сортировка** - робот перемещает пробирки в соответствующие штативы назначения

Поддерживаемые типы анализов:
- **УГИ** (pcr-1) - Урогенитальные инфекции
- **ВПЧ** (pcr-2) - Вирус папилломы человека
- **УГИ+ВПЧ** (pcr-1+pcr-2) - Комбинированный анализ
- **Разное** (pcr) - Прочие анализы

## ✨ Возможности

- 🤖 **Веб-интерфейс управления** - удобное управление роботом через браузер
- 📊 **Визуальная настройка матриц** - графический редактор расположения пробирок
- ⏯️ **Управление выполнением** - старт/стоп/пауза программы в реальном времени
- 🔄 **Динамическая замена штативов** - уведомления о необходимости замены заполненных штативов
- 📋 **Просмотр баркодов** - отображение отсканированных пробирок по штативам
- 🔌 **Асинхронная обработка** - параллельные запросы к ЛИС для ускорения работы
- 🎯 **Умное сканирование** - до 10 попыток для надежного распознавания QR-кодов
- 📝 **Логирование** - детальная информация о каждой операции

## 🏗️ Архитектура

```
┌─────────────────┐
│  Веб-интерфейс  │ ← Управление через браузер
└────────┬────────┘
         │
┌────────▼────────┐
│  Flask Server   │ ← API и веб-сервер
└────────┬────────┘
         │
┌────────▼────────────┐
│ Robot Controller    │ ← Управление состоянием
└────────┬────────────┘
         │
┌────────▼────────────┐
│   Main Program      │ ← Основная логика
└───┬────────────┬────┘
    │            │
┌───▼───┐    ┌───▼────┐
│ Robot │    │Scanner │ ← Аппаратура
└───────┘    └────────┘
         │
    ┌────▼─────┐
    │   ЛИС    │ ← Информация о тестах
    └──────────┘
```

## 📦 Требования

### Аппаратное обеспечение
- Робот-манипулятор **Agilebot GBT-C5A**
- QR-сканер с TCP/IP интерфейсом
- Компьютер управления (Windows/Linux)

### Программное обеспечение
- Python 3.8+
- Agilebot Python SDK
- Веб-браузер (Chrome, Firefox, Edge)

### Python-пакеты
```
flask>=2.0.0
flask-cors>=3.0.0
aiohttp>=3.8.0
asyncio
```

## 🚀 Установка

### 1. Клонирование репозитория
```bash
git clone <repository-url>
cd roboarm
```

### 2. Установка зависимостей
```bash
pip install flask flask-cors aiohttp --break-system-packages
```

### 3. Установка Agilebot SDK
Следуйте инструкциям производителя для установки SDK робота.

### 4. Конфигурация сети
Убедитесь, что устройства доступны в сети:
- **Робот**: `192.168.124.2`
- **Сканер**: `192.168.124.4:6000`
- **ЛИС**: `127.0.0.1:7114` (или IP вашего сервера ЛИС)

## ⚡ Быстрый старт

### 1. Запуск веб-сервера
```bash
python server.py
```

Сервер запустится на `http://localhost:5000`

### 2. Открытие веб-интерфейса
Откройте браузер и перейдите по адресу:
```
http://localhost:5000
```

### 3. Настройка матриц штативов
1. Нажмите **"⚙️ Настройки штативов"**
2. Нарисуйте расположение пробирок на матрицах
3. Нажмите **"Заполнить внутренние области"**
4. Сохраните конфигурацию кнопкой **"Сохранить расстановку"**

### 4. Запуск программы
1. Вернитесь на главную страницу
2. Нажмите **"▶️ Старт программы"**
3. Робот начнет работу автоматически

### 5. Управление процессом
- **⏸️ Пауза** - приостановить выполнение
- **▶️ Продолжить** - возобновить работу
- **⏹️ Остановка** - полная остановка программы

## ⚙️ Конфигурация

### Настройка координат роботов

В файле `main.py` настройте координаты:

```python
# Исходные паллеты
source_pallets_config = [
    {
        'id': 0,
        'matrix': matrix1,
        'scan_position': (175, 280, 200),  # X, Y, Z для сканирования
        'sort_position': (129, 317, 148),   # X, Y, Z для забора
    }
]

# Паллеты назначения
dest_positions = {
    0: (-93, 317, 146),    # УГИ #1
    1: (-315, 317, 146),   # ВПЧ
    2: (-537, 317, 146),   # УГИ+ВПЧ
    3: (-759, 317, 146),   # Разное
    4: (-981, 317, 146),   # УГИ #2
}

# Позиция для замены штативов
PAUSE_POSITION = (-512, 310, 195)
```

### Настройка ЛИС

```python
LIS_HOST = "192.168.12.80"  # IP-адрес сервера ЛИС
LIS_PORT = 7117              # Порт сервера ЛИС
```

### Параметры сканирования

```python
# Расстояние между пробирками (мм)
X_STEP = 20.7
Y_STEP = 20.7

# Вместимость штативов
RACK_CAPACITY = 50  # пробирок на штатив
```

## 📖 Использование

### Веб-интерфейс

#### Главная страница (`/`)
- **Настройки штативов** - переход к редактору матриц
- **Старт программы** - запуск основного цикла
- **Пауза/Продолжить** - управление выполнением
- **Полная остановка** - аварийная остановка

#### Управление штативами
- **Количество пробирок** - укажите, сколько пробирок в штативе
- **Просмотр баркодов** - показывает отсканированные QR-коды
- **Заменить штатив** - уведомляет систему о замене штатива

#### Редактор матриц (`/matrices.html`)
1. Зажмите мышь и проведите по ячейкам для рисования контура
2. Двойной клик на ячейке снимает выделение
3. **"Заполнить внутренние области"** - автоматически заполняет замкнутый контур
4. **"Сохранить расстановку"** - сохраняет конфигурацию в `matrix_data.py`

### Консольный режим

Для запуска без веб-интерфейса:
```bash
python main.py
```

## 🔌 API

### Управление программой

#### Запуск программы
```http
POST /api/start_program
```
**Ответ:**
```json
{
  "success": true,
  "message": "Программа запускается..."
}
```

#### Остановка программы
```http
POST /api/stop_program
```

#### Пауза программы
```http
POST /api/pause_program
```

#### Возобновление программы
```http
POST /api/resume_program
```

### Управление штативами

#### Замена штатива
```http
POST /api/change_rack
Content-Type: application/json

{
  "type": "ugi",
  "rack_id": 0,
  "tube_count": 50
}
```

#### Статус робота
```http
GET /api/robot_status
```
**Ответ:**
```json
{
  "running": true,
  "paused": false,
  "rack_to_change": null,
  "command": "idle",
  "rack_counts": {
    "ugi": 2,
    "vpch": 1,
    "both": 1,
    "other": 1
  }
}
```

#### Получение баркодов
```http
GET /api/get_barcodes
```
**Ответ:**
```json
{
  "success": true,
  "barcodes": {
    "ugi": {
      "0": ["BC001", "BC002", ...]
    },
    "vpch": {
      "1": ["BC101", "BC102", ...]
    }
  }
}
```

### Сохранение матриц

#### Сохранение конфигурации матриц
```http
POST /save_matrix
Content-Type: application/json

{
  "content": "# Python код матриц",
  "filename": "matrix_data.py"
}
```

## 📁 Структура проекта

```
roboarm/
├── main.py                 # Основная программа сортировки
├── server.py               # Flask веб-сервер и API
├── robot_controller.py     # Управление состоянием робота
├── RobotManipulator.py     # Обертка для SDK Agilebot
├── scanner.py              # Управление QR-сканером
├── matrix_data.py          # Конфигурация матриц штативов
│
├── index.html              # Главная страница веб-интерфейса
├── matrices.html           # Редактор матриц
├── style.css               # Стили для веб-интерфейса
│
├── test_server.py          # Эмулятор сервера ЛИС
├── test.py                 # Тестовые HTTP-запросы
│
├── robot_state.json        # Состояние робота (генерируется)
├── robot_data.json         # Данные о пробирках (генерируется)
│
├── .gitignore
└── README.md
```

### Описание основных модулей

#### `main.py`
Основная логика программы:
- Сканирование пробирок с паллетов
- Асинхронные запросы к ЛИС
- Классификация пробирок по типам тестов
- Сортировка и перемещение пробирок

#### `server.py`
Flask веб-сервер:
- REST API для управления роботом
- Обслуживание веб-интерфейса
- Сохранение конфигурации матриц

#### `robot_controller.py`
Управление состоянием:
- Синхронизация между веб-интерфейсом и программой
- Обработка команд старт/стоп/пауза
- Управление заменой штативов

#### `RobotManipulator.py`
Обертка для Agilebot SDK:
- Подключение к роботу
- Управление движениями
- Работа с регистрами и сигналами

#### `scanner.py`
Управление QR-сканером:
- TCP/IP соединение со сканером
- Запуск и остановка сканирования
- Обработка результатов

## 🔧 Устранение неполадок

### Робот не подключается
```
Ошибка: Ошибка подключения к роботу
```
**Решение:**
1. Проверьте IP-адрес робота (по умолчанию `192.168.124.2`)
2. Убедитесь, что робот включен и находится в сети
3. Проверьте сетевое подключение: `ping 192.168.124.2`
4. Убедитесь, что Agilebot SDK установлен корректно

### Сканер не отвечает
```
Ошибка: Connection timeout
```
**Решение:**
1. Проверьте IP и порт сканера (`192.168.124.4:6000`)
2. Убедитесь, что сканер включен
3. Проверьте сетевое подключение: `ping 192.168.124.4`
4. Проверьте, что порт 6000 не заблокирован файрволом

### ЛИС недоступна
```
Ошибка: Таймаут при запросе баркода
```
**Решение:**
1. Проверьте параметры `LIS_HOST` и `LIS_PORT` в `main.py`
2. Убедитесь, что ЛИС работает
3. Для тестирования запустите тестовый сервер:
   ```bash
   python test_server.py
   ```
4. Измените настройки в `main.py`:
   ```python
   LIS_HOST = "127.0.0.1"
   LIS_PORT = 7114
   ```

### Ошибки сканирования
```
Предупреждение: NoRead (ожидалась пробирка)
```
**Решение:**
1. Проверьте качество QR-кодов на пробирках
2. Убедитесь, что пробирки установлены правильно
3. Отрегулируйте координаты сканирования в `main.py`
4. Увеличьте количество попыток:
   ```python
   scan_three_positions(..., max_attempts=15)
   ```
5. Проверьте освещение и угол обзора сканера

### Матрица не сохраняется
```
Ошибка: убедись что сервер запущен
```
**Решение:**
1. Убедитесь, что `server.py` запущен
2. Проверьте путь `SAVE_DIRECTORY` в `server.py`
3. Убедитесь, что у вас есть права на запись в каталог
4. Проверьте консоль браузера (F12) на наличие ошибок

### Программа зависает на паузе
**Решение:**
1. Проверьте файл `robot_state.json`
2. Нажмите кнопку "Продолжить" в веб-интерфейсе
3. При необходимости удалите `robot_state.json` и перезапустите сервер
4. Проверьте логи в консоли на наличие ошибок

### Веб-интерфейс не загружается
**Решение:**
1. Убедитесь, что сервер запущен: `python server.py`
2. Проверьте, что порт 5000 свободен
3. Попробуйте другой порт в `server.py`:
   ```python
   app.run(debug=True, port=8080, host='0.0.0.0')
   ```
4. Откройте браузер в режиме инкогнито

## 🧪 Тестирование

### Запуск тестового сервера ЛИС
```bash
python test_server.py
```
Сервер эмулирует ответы ЛИС на порту `7114` и возвращает случайные типы тестов.

**Проверка работы:**
```bash
curl -X POST http://localhost:7114/get_tests \
  -H "Content-Type: application/json" \
  -d '{"mes_type": "LA", "tube_barcode": "TEST123"}'
```

### Проверка HTTP-запросов к ЛИС
```bash
python test.py
```

### Тестирование без оборудования
1. Закомментируйте подключение к роботу в `main.py`:
   ```python
   # connect_devices(scanner, cobot)
   ```
2. Используйте тестовый сервер ЛИС
3. Запустите программу для проверки логики

### Проверка API
```bash
# Проверка статуса
curl http://localhost:5000/api/robot_status

# Запуск программы
curl -X POST http://localhost:5000/api/start_program

# Пауза программы
curl -X POST http://localhost:5000/api/pause_program
```

## 📝 Примечания

### Технические ограничения
- Максимальная вместимость штатива: **50 пробирок** (10 рядов × 5 колонок)
- Поддержка до **6 паллетов назначения** одновременно
- Асинхронная обработка запросов к ЛИС для ускорения
- Автоматическое сохранение состояния для восстановления после сбоев
- До **10 попыток** сканирования каждой пробирки для надежности

### Рекомендации по эксплуатации
1. **Регулярно калибруйте** координаты робота
2. **Проверяйте качество** QR-кодов перед началом работы
3. **Следите за состоянием** захватного механизма
4. **Делайте резервные копии** файлов конфигурации
5. **Мониторьте логи** для раннего выявления проблем

### Безопасность
- Робот автоматически останавливается при ошибках
- Аварийная кнопка остановки в веб-интерфейсе
- Автоматическая пауза при замене штативов
- Логирование всех операций для аудита

## 🔄 Обновления и поддержка

### Планируемые улучшения
- [ ] Поддержка нескольких исходных паллетов
- [ ] Статистика и отчеты
- [ ] Уведомления по email/Telegram
- [ ] Автоматическая калибровка координат
- [ ] Распознавание типа пробирки по изображению
- [ ] Интеграция с базой данных

### История версий
- **v1.0** (2025) - Первый релиз
  - Базовая функциональность сканирования и сортировки
  - Веб-интерфейс управления
  - Интеграция с ЛИС

## 🤝 Поддержка

При возникновении проблем:
1. Проверьте раздел [Устранение неполадок](#устранение-неполадок)
2. Проверьте логи в консоли
3. Убедитесь, что все устройства подключены
4. Проверьте файлы состояния (`robot_state.json`, `robot_data.json`)
5. Перезапустите сервер при необходимости

### Контакты
- Email: support@example.com
- Документация: https://docs.example.com
- Issues: https://github.com/example/roboarm/issues

## 📄 Лицензия

Проект разработан для автоматизации лабораторных процессов.

---

**Версия:** 1.0  
**Дата:** 2025  
**Совместимость:** Agilebot GBT-C5A  
**Язык:** Python 3.8+

## 🙏 Благодарности

- Agilebot за предоставление SDK
- Команда разработчиков Flask
- Сообщество Python

---

Made with ❤️ for laboratory automation